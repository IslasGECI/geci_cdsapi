services:
  islasgeci:
    build:
      context: .
      dockerfile: Dockerfile
    image: islasgeci/geci_cdsapi:latest
    command: bash
    volumes:
      - $PWD:/workdir
    environment:
      CDSAPI_KEY: $CDSAPI_KEY
